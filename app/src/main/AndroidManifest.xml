<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Location permissions for job search -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    
    <!-- Storage permissions for file uploads -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    
    <!-- Camera permission for profile photos -->
    <uses-permission android:name="android.permission.CAMERA" />
    
    <!-- Phone permission for contact -->
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:name=".ChotuJobsApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChotuJobs"
        android:usesCleartextTraffic="true">
        
        <!-- Splash Activity -->
        <activity
            android:name=".ui.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.ChotuJobs.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Main Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.ChotuJobs.NoActionBar" />
        
        <!-- Authentication Activity -->
        <activity
            android:name=".ui.auth.SimpleAuthActivity"
            android:exported="false"
            android:theme="@style/Theme.ChotuJobs.NoActionBar" />
        
        <!-- Laborer Activities -->
        <activity
            android:name=".ui.laborer.LaborerDashboardActivity"
            android:exported="false"
            android:theme="@style/Theme.ChotuJobs.NoActionBar" />
        
        <!-- Contractor Activities -->
        <activity
            android:name=".ui.contractor.ContractorDashboardActivity"
            android:exported="false"
            android:theme="@style/Theme.ChotuJobs.NoActionBar" />
        
        <!-- Agent Activities -->
        <activity
            android:name=".ui.agent.AgentDashboardActivity"
            android:exported="false"
            android:theme="@style/Theme.ChotuJobs.NoActionBar" />

        <!-- File Provider for image capture -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        
    </application>

</manifest>